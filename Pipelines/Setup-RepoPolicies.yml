trigger: none

parameters:
- name: agentType
  displayName: 'Choose agent to run pipeline'
  default: 'on-prem'
  values:
  - 'cloud'
  - 'on-prem'

steps:
- template: Utils/Run-AzDevOpsCLI.yml
  parameters:
    agentType: ${{ parameters.agentType }}
